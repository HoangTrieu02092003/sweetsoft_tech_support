@model IEnumerable<admin_sweetsoft_tech_support.Models.TblUserPermission>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Permission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Permission.PermissionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserPermissionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserPermissionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserPermissionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
