@model IEnumerable<admin_sweetsoft_tech_support.Models.NotificationEntry>

@{
    ViewData["Title"] = "Thông báo của tôi";
    int totalPages = (int)ViewData["TotalPages"];
    int currentPage = (int)ViewData["CurrentPage"];
    int stt = 1;
}

<h1 class="text-center">Thông báo của tôi</h1>

@if (!Model.Any())
{
    <h5>Hiện tại chưa có thông báo nào.</h5>
}
else
{
    <div class="table-responsive mt-4">
        <table class="table table-striped" id="notificationList">
            <thead>
                <tr>
                    <th>STT</th>
                    <th class="text-center">Người nhận</th>
                    <th class="text-center">Nội dung</th>
                    <th class="text-center d-none d-md-table-cell">Thời gian</th>
                    <th class="text-center">Trạng thái</th>
                    <th class="text-center">Hành động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var myNoti in Model)
                {
                    <tr >
                        <td>@stt</td>
                        <td class="text-center">@myNoti.User</td>
                        <td class="text-center">@myNoti.Title</td>
                        <td class="text-center d-none d-md-table-cell">@myNoti.Timestamp.ToString("dd/MM/yyyy HH:mm:ss")</td>
                        <td class="text-center">
                            @if (myNoti.Status == "1")
                            {
                                @:Đã xem
                            }
                            else
                            {
                                <strong>Chưa xem</strong>
                            }
                        </td>
                        <td class="text-center">    
                            <div class="d-inline-flex gap-2">
                                <button type="submit" class="btn btn-sm btn-outline-info" data-bs-toggle="modal" data-bs-target="#detailModal-@myNoti.Id">
                                    <i class="ti ti-eye-check"></i>
                                </button>
                                <form asp-action="DeleteNotification" asp-route-id="@myNoti.Id" method="post" style="display:inline-block" onsubmit="return confirmDelete();">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="ti ti-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    stt++;
                    //Modal chi tiết
                    <div class="modal fade" id="detailModal-@myNoti.Id" tabindex="-1" aria-labelledby="detailModalLabel-@myNoti.Id" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h3 class="modal-title" id="detailModalLabel-@myNoti.Id">Chi tiết thông báo</h3>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <p><strong>Người nhận:</strong> @myNoti.User</p>
                                    <p><strong>Tiêu đề:</strong> @myNoti.Title</p>
                                    <p><strong>Nội dung:</strong> @myNoti.Content</p>
                                    <p><strong>Thời gian:</strong> @myNoti.Timestamp</p>
                                    <p><strong>Trạng thái:</strong> @(myNoti.Status == "1" ? "Đã xem" : "Chưa xem")</p>
                                </div>
                                <div class="modal-footer">
                                    <form asp-action="MarkAsRead" asp-route-id="@myNoti.Id" method="post">
                                        <button type="submit" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </tbody>
        </table>
    </div>
}


<!-- Phân trang -->
<div class="d-flex justify-content-center my-4" id="dbPagination">
    <ul class="pagination">
        @if (totalPages > 1)
        {
            if (currentPage > 1)
            {
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("MyNotifications", new { page = 1, date = ViewData["Date"], filterOption = ViewData["FilterOption"], searchTerm = ViewData["SearchTerm"]})"><i class="bi bi-chevron-double-left"></i></a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("MyNotifications", new { page = currentPage - 1, date = ViewData["Date"], filterOption = ViewData["FilterOption"], searchTerm = ViewData["SearchTerm"] })"><i class="bi bi-chevron-left"></i></a>
                </li>
            }
            for (int i = 1; i <= totalPages; i++)
            {
                <li class="page-item @(i == currentPage ? "active" : "")">
                    <a class="page-link" href="@Url.Action("MyNotifications", new { page = i, date = ViewData["Date"], filterOption = ViewData["FilterOption"], searchTerm = ViewData["SearchTerm"] })">@i</a>
                </li>
            }
            if (currentPage < totalPages)
            {
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("MyNotifications", new { page = currentPage + 1, date = ViewData["Date"], filterOption = ViewData["FilterOption"], searchTerm = ViewData["SearchTerm"] })"><i class="bi bi-chevron-right"></i></a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("MyNotifications", new { page = totalPages, date = ViewData["Date"], filterOption = ViewData["FilterOption"], searchTerm = ViewData["SearchTerm"] })"><i class="bi bi-chevron-double-right"></i></a>
                </li>
            }
        }
    </ul>
</div>

<script>
    function confirmDelete() {
        return confirm("Bạn có chắc chắn muốn xóa thông báo này không?");
    }
</script>