@model List<dynamic>

@if (Model.Any())
{
    <span class="notification-tooltip">@Model.FirstOrDefault()?.Message</span>
}
else
{
    <span class="notification-tooltip">Không có thông báo nào</span>
}